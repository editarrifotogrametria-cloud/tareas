# =============================================================================
# Configuración de GNSS.AI
# =============================================================================
# Copiar este archivo a .env y ajustar según tu sistema
#
# cp .env.example .env
#
# =============================================================================

# Puerto GPS para ComNav K222
# -----------------------------------------------------------------------------
# Windows: GPS_PORT=COM3
# Linux USB: GPS_PORT=/dev/ttyUSB2 (COM3 = tercer puerto USB)
# Linux ACM: GPS_PORT=/dev/ttyACM2 (COM3 = tercer puerto ACM)
# Raspberry Pi: GPS_PORT=/dev/serial0
# -----------------------------------------------------------------------------
GPS_PORT=COM3

# Velocidad del puerto serial (baud rate)
GPS_BAUD=115200

# Servidor
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
